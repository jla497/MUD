image: dewert/gibanica:base
stages:
  - build

after_script:
  - rm -rf build
  
stages:
  - build
  - test
  - deploy

compile:
  stage: build
  script:
    - mkdir build
    - cd build
    - cmake ../MudServer
    - make
    - cd ..

connection-manager-unit-test:
  stage: test
  script:
    - cd build/bin
    - ./runAllTests | grep "PASSED"


